# Generated by engineering-professor. This workflow is formative feedback.
# Final grading repeats critical checks in an instructor-controlled runner.
name: Week 04 Academic Feedback

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: week-04-${{ github.ref }}
  cancel-in-progress: true

jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with: 
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 2
          fetch-tags: true
      - name: Fetch tags
        run: git fetch --tags --force
      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Reproducible setup
        run: make setup
      - name: Public Expo Android build
        run: npm run bundle:release
      - name: AC-01 — reproducibility
        run: make verify-week-04
      - name: AC-02 — public behavior
        run: make public-test-week-04
      - name: AC-03 — declared failure evidence
        run: make evidence-week-04
      - name: Upload academic evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: academic-evidence-week-04-seguridad-privacidad
          if-no-files-found: warn
          path: |
            reports/week-04/**
            evidence/week-04/**

